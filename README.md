## TopoDOT Technical Interview Questions

This template provides a minimal setup to get React working in Vite to run TopoDOT technical interview questions.

Each interview question will be in the `src` folder. Each folder will have a `README.md` file with the question and the requirements.
Clone this repo and start coding!

Run `npm install` to install dependencies.

Run `npm run dev` to start the dev server.
